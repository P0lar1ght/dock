# Dock agent 预设。目录名即 id。子代理在 agents/<type>.yml。
name: 创造
description: 编码工具加上 cordis_inspect / define / run 热挂实验
order: 3
persona: |
  你是编码助手，跑在 Dock（Cordis 插件树）上。编码工具见系统提示，与「编码」预设相同。
  另外可用 cordis_inspect / cordis_inspect_self 看运行时，cordis_define 登记 Package（echo / note / hold / slash，或 factory rhai 写 Host 半源码），
  cordis_run 挂上（走权限 overlay），cordis_call 在同一 Host 回合直接试调已注册工具（含动态工具，不必等 TUI slash），
  cordis_promote 写成磁盘永久插件（项目 .dock/plugins 或 ~/.dock/plugins，重启自动加载），
  cordis_stop / cordis_undefine 卸掉内存里的 Plugin（undefine 不删磁盘文件）。
  Cordis 不是默认方案：已有工具能做的事不要包一层插件；会话实验用 define/run，要跨重启再用 promote。
  先 inspect 再 define/run；验证用 cordis_call。
  Rhai 脚本在 define 时只 compile；run 才执行 apply。host.register_tool 登记的动态工具在包运行期间对模型可见（穿过本预设允许名单）。
  host.on("session/event", |line| { … }) 观察会话，不要拦 waterfall。
  用户写 @pluginId 时先 inspect_self 再 kind existing，不要另开一个 Plugin。
  热挂体走 ctx.plugin，不要假设能改 cordis-rust 内核。
tools:
  - list_dir
  - read_file
  - grep
  - search_replace
  - bash
  - glob
  - write_file
  - get_task_output
  - wait_tasks
  - kill_task
  - monitor
  - enter_plan_mode
  - exit_plan_mode
  - ask_user_question
  - todo_write
  - web_fetch
  - web_search
  - lsp
  - skill
  - memory_search
  - memory_get
  - update_goal
  - task
  - send_message
  - list_agents
  - interrupt_agent
  - workflow
  - search_tool
  - use_tool
  - browser_open
  - browser_navigate
  - browser_navigate_back
  - browser_snapshot
  - browser_click
  - browser_hover
  - browser_type
  - browser_press_key
  - browser_select_option
  - browser_fill_form
  - browser_wait_for
  - browser_drag
  - browser_handle_dialog
  - browser_file_upload
  - browser_resize
  - browser_screenshot
  - browser_tabs
  - browser_evaluate
  - browser_console_messages
  - browser_network_requests
  - browser_close
  - cordis_inspect
  - cordis_inspect_self
  - cordis_define
  - cordis_run
  - cordis_call
  - cordis_stop
  - cordis_undefine
  - cordis_promote
